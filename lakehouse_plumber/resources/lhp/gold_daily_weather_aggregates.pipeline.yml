# Generated by LakehousePlumber - Bundle Resource for gold_daily_weather_aggregates
resources:
  pipelines:
    gold_daily_weather_aggregates_pipeline:
      name: gold_daily_weather_aggregates_pipeline
      catalog: main
      schema: lhp_${bundle.target}
      libraries:
        - notebook:
            path: ../../generated/gold_daily_weather_aggregates/weather_analytics.py
      configuration:
        bundle.sourcePath: ${workspace.file_path}/generated
